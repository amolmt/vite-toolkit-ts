enum ButtonSize {
  SMALL = 'small',
  MIDDLE = 'middle',
  LARGE = 'large',
}

enum ButtonShape {
  DEFAULT = 'default',
  CIRCLE = 'circle',
  ROUND = 'round',
}

enum ButtonType {
  PRIMARY = 'primary',
  DEFAULT = 'default',
  DASHED = 'dashed',
  TEXT = 'text',
  LINK = 'link',
}

export { ButtonSize, ButtonShape, ButtonType };
